/*
    Rewrite
    let result;

    if (a + b < 4) {
        result = 'Below';
    } else {
        result = 'Over';
    }

    using the conditional operator '?'
*/

let result;
result = (a + b < 4) ? 'Below' : 'Over';