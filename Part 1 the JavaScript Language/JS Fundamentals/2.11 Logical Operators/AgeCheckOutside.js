/*
    Write an if condition to check that age
    is not between 14 and 90 inclusively
*/

// First variant using !
if(!(age >= 14 && age <= 90))

// Second variant without !
if (age < 14 || age > 90)